# 列过滤功能 - 右键点击版本

## 功能说明

在Tenant CI/CD对话框的结果表格中，以下三列支持过滤功能：
- **Image Name** (第2列)
- **Version** (第5列)  
- **Git Branch** (第6列)

## 使用方法

### 触发过滤
**右键点击**列标题即可打开过滤对话框

### Image Name 过滤
1. 右键点击 "Image Name" 列标题
2. 在弹出的对话框中输入关键词
3. 支持模糊匹配（不区分大小写，部分匹配）
4. 点击 "OK" 应用过滤，或 "Clear" 清除过滤

### Version 过滤
1. 右键点击 "Version" 列标题
2. 在弹出的对话框中输入关键词
3. 支持模糊匹配（不区分大小写，部分匹配）
4. 点击 "OK" 应用过滤，或 "Clear" 清除过滤

### Git Branch 过滤
1. 右键点击 "Git Branch" 列标题
2. 从下拉列表中选择分支
3. 支持输入过滤（实时过滤分支列表）
4. 点击 "OK" 应用过滤，或 "Clear" 清除过滤

## 组合过滤

- 三个过滤器可以同时使用
- 使用 AND 逻辑：结果必须同时满足所有已设置的过滤条件
- 例如：可以同时过滤特定的镜像名称、版本号和分支

## 排序功能

- **左键点击**列标题可以触发排序
- 右键点击用于过滤，不会触发排序
- 排序和过滤功能互不干扰

## 实现细节

### 代码变更
- 移除了 [F] 图标及其相关的自定义表头渲染器
- 简化为仅使用右键点击监听器
- 减少了代码复杂度，提高了可靠性

### 优势
1. **简洁**：无需额外的UI元素
2. **可靠**：右键点击事件不会被自定义渲染器干扰
3. **直观**：符合常见的右键菜单操作习惯
4. **无冲突**：左键排序、右键过滤，功能分离

## 测试步骤

1. 启动应用程序
2. 打开 Tenant CI/CD 对话框
3. 连接到租户并执行搜索
4. 右键点击 Image Name、Version 或 Git Branch 列标题
5. 验证过滤对话框正常弹出
6. 输入过滤条件并应用
7. 验证表格数据正确过滤
8. 左键点击列标题验证排序功能正常

## 编译和运行

```bash
# 编译打包
mvn clean package -DskipTests

# 运行
java -jar target/git-info-viewer-1.0.0-jar-with-dependencies.jar
```

## 文件变更

- `src/main/java/com/gitviewer/TenantCICDDialog.java`
  - 简化了 `addColumnFilterIcons()` 方法
  - 移除了 `addFilterIconToColumn()` 方法
  - 简化了 `handleHeaderClick()` 为 `handleHeaderRightClick()`
  - 移除了所有调试输出代码
