# 控制台日志输出说明

## 问题
用户反馈 "SLF4J NOP" 输出看不到，需要在控制台看到日志。

## 解决方案

### ✅ 已完成的配置

1. **SLF4J Simple Logger 配置文件**
   - 位置：`src/main/resources/simplelogger.properties`
   - 配置：输出到 System.out（控制台）
   - 日志级别：DEBUG（用于 PortalApiClient、TenantCICDDialog、BuildOutputDialog）

2. **双重日志输出**
   - 所有关键代码同时使用：
     - `logger.info()` / `logger.debug()` / `logger.error()`
     - `System.out.println()` / `System.err.println()`
   - 这样即使 SLF4J 配置有问题，也能看到 System.out 的输出

3. **详细的日志内容**
   - API 请求详情（方法、URL、请求头、请求体）
   - API 响应详情（状态码、响应体预览）
   - Plan 查询详情（所有 plan 名称、匹配逻辑）
   - 错误日志（带堆栈跟踪）

### 🎯 如何看到控制台输出

**重要：必须使用以下方法之一才能看到控制台输出！**

#### 方法 1：使用 `run-with-console.bat`（推荐）
```batch
run-with-console.bat
```
- 保持控制台窗口打开
- 显示所有日志输出
- 应用关闭后控制台不会立即消失

#### 方法 2：使用 `test-logging.bat`（新增）
```batch
test-logging.bat
```
- 专门用于测试日志输出
- 与方法 1 类似，但有更清晰的提示信息

#### 方法 3：手动运行
```batch
java -jar target\git-info-viewer-1.0.0-jar-with-dependencies.jar
```
- 直接在命令行运行
- 所有输出显示在当前控制台

### ❌ 不要使用

**不要使用 `restart-app.bat` 来查看日志！**
- 它使用 `start` 命令启动应用
- 应用在后台窗口运行
- 控制台输出不可见

## 验证步骤

### 1. 验证配置
```batch
verify-logging-config.bat
```
这会检查：
- simplelogger.properties 文件是否存在
- JAR 文件是否已构建
- 显示正确的运行方法

### 2. 重新构建（如果需要）
```batch
mvn clean package
```

### 3. 使用正确的方法运行
```batch
run-with-console.bat
```
或
```batch
test-logging.bat
```

### 4. 测试 Tenant CI/CD 功能
1. 点击 "Tenant CI/CD" 按钮
2. 点击 "Connect" - 观察连接日志
3. 输入 plan 名称并点击 "Search" - 观察 API 调用日志
4. 双击结果行 - 观察构建输出日志

## 日志输出示例

### 连接时的日志
```
=== Getting Token ===
Username: xxx, TenantCode: xxx
=== POST Request ===
URL: https://portal.insuremo.com/cas/get-token
Header: Content-Type = application/json
Header: x-mo-user-source-id = platform
Header: x-mo-tenant-id = xxx
Header: x-mo-client-id = key
Body: [REDACTED - contains sensitive data]
=== Response (Success) ===
Status Code: 200
Body: {"access_token":"...","expire_in":7200,...}
```

### 按 Plan 搜索时的日志
```
=== Getting Plan Names ===
TenantCode: xxx
=== GET Request ===
URL: https://portal.insuremo.com/api/mo-fo/1.0/ops/multi_build/title_list
Header: x-mo-target-tenant = xxx
Header: authorization = Bearer abcd...wxyz
Header: Accept = application/json
=== Response (Success) ===
Status Code: 200
Body: ["Plan 1","Plan 2","Plan 3",...]
Parsed 10 plan names
  Plan[0]: Plan 1
  Plan[1]: Plan 2
  Plan[2]: Plan 3
  ...
```

### 查看构建输出时的日志
```
=== Getting Build Output by ID ===
TenantCode: xxx, BuildId: 12345
Full URL: https://portal-gw.insuremo.com/eBao/1.0/ops/build/query_one?id=12345
Request headers: x-mo-target-tenant=xxx, authorization=Bearer abcd...wxyz
=== GET Request ===
URL: https://portal-gw.insuremo.com/eBao/1.0/ops/build/query_one?id=12345
Header: x-mo-target-tenant = xxx
Header: authorization = Bearer abcd...wxyz
Header: Accept = application/json
=== Response (Success) ===
Status Code: 200
Response received, length: 5432
Response preview (first 500 chars): {"callback":{"build_output":"..."},...}
```

## 技术细节

### SLF4J Simple Logger 配置
文件：`src/main/resources/simplelogger.properties`

```properties
# 输出到控制台
org.slf4j.simpleLogger.logFile=System.out

# 默认日志级别
org.slf4j.simpleLogger.defaultLogLevel=INFO

# 显示时间戳
org.slf4j.simpleLogger.showDateTime=true
org.slf4j.simpleLogger.dateTimeFormat=yyyy-MM-dd HH:mm:ss

# 特定类的日志级别（DEBUG 显示详细信息）
org.slf4j.simpleLogger.log.com.gitviewer.PortalApiClient=DEBUG
org.slf4j.simpleLogger.log.com.gitviewer.TenantCICDDialog=DEBUG
org.slf4j.simpleLogger.log.com.gitviewer.BuildOutputDialog=DEBUG
```

### 双重日志实现
在 `PortalApiClient.java` 中：
```java
private void logRequest(String method, String url, Map<String, String> headers, String body) {
    String logMsg = "=== " + method + " Request ===";
    logger.info(logMsg);           // SLF4J 日志
    System.out.println(logMsg);    // 直接控制台输出
    
    // ... 更多日志
}
```

这样即使 SLF4J 配置有问题，`System.out.println()` 也能保证输出到控制台。

## 故障排除

### 如果仍然看不到输出

1. **检查运行方法**
   - 确认使用的是 `run-with-console.bat` 或 `test-logging.bat`
   - 不要使用 `restart-app.bat`

2. **检查 JAR 文件**
   - 运行 `verify-logging-config.bat` 验证配置
   - 如果需要，重新构建：`mvn clean package`

3. **检查控制台编码**
   - 如果看到乱码（中文字符），运行：
     ```batch
     chcp 65001
     ```
   - 然后重新运行应用

4. **手动验证**
   - 在命令行直接运行：
     ```batch
     java -jar target\git-info-viewer-1.0.0-jar-with-dependencies.jar
     ```
   - 应该能看到所有输出

## 总结

✅ **日志配置已正确完成**
- SLF4J Simple Logger 配置文件已创建
- 双重日志输出（SLF4J + System.out）已实现
- 详细的 API 调用日志已添加

✅ **使用正确的运行方法**
- 使用 `run-with-console.bat` 或 `test-logging.bat`
- 不要使用 `restart-app.bat`（它会隐藏控制台）

✅ **验证工具已提供**
- `verify-logging-config.bat` - 验证配置
- `test-logging.bat` - 测试日志输出

现在运行 `test-logging.bat` 或 `run-with-console.bat`，你应该能看到所有的日志输出！
