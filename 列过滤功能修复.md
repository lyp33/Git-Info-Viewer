# 列过滤功能修复

## 问题描述

1. **新增的两列点击 [F] 没反应**
   - Image Name 列和 Version 列的过滤图标点击无响应
   
2. **点击 [F] 时触发了排序**
   - 点击过滤图标时会触发表格的自动排序功能

## 修复内容

### 1. 禁用自动排序

**修改位置：** `TenantCICDDialog.java` - `createResultsPanel()` 方法

**修改前：**
```java
resultsTable = new JTable(tableModel);
resultsTable.setAutoCreateRowSorter(true);  // 启用自动排序
```

**修改后：**
```java
resultsTable = new JTable(tableModel);
resultsTable.setAutoCreateRowSorter(false);  // 禁用自动排序，避免点击表头时触发排序
```

**原因：** 自动排序会在点击表头时触发，与过滤图标的点击事件冲突。

### 2. 改进鼠标监听器

**修改位置：** `TenantCICDDialog.java` - `addColumnFilterIcons()` 方法

**改进内容：**

1. **增加调试日志**
   ```java
   logger.debug("Table header clicked: column={}, x={}, y={}", column, e.getX(), e.getY());
   logger.debug("Header rect: x={}, width={}, iconX={}, clickX={}", 
              headerRect.x, headerRect.width, iconX, e.getX());
   logger.info("Filter icon clicked for column {}", column);
   ```

2. **优化点击区域检测**
   ```java
   // 只处理有过滤图标的列
   if (column == 2 || column == 5 || column == 6) {
       Rectangle headerRect = resultsTable.getTableHeader().getHeaderRect(column);
       int iconX = headerRect.x + headerRect.width - 35;  // 从右侧35像素开始
       
       // 检查点击位置是否在图标区域
       if (e.getX() >= iconX) {
           // 处理过滤
           e.consume();  // 消费事件，防止触发其他操作
       }
   }
   ```

3. **增加事件消费**
   - 添加 `e.consume()` 防止事件继续传播

4. **扩大图标点击区域**
   - 从 30 像素改为 35 像素，更容易点击

## 测试验证

### 测试步骤

1. 启动应用程序
2. 打开 Tenant CI/CD 对话框
3. 执行搜索获取结果
4. 点击 Image Name 列头的 [F] 图标
5. 点击 Version 列头的 [F] 图标
6. 点击 Git Branch 列头的 [F] 图标

### 预期结果

- ✅ 点击 [F] 图标时打开对应的过滤对话框
- ✅ 不触发表格排序
- ✅ 控制台输出调试日志（如果启用）
- ✅ 过滤功能正常工作

### 调试日志示例

```
[DEBUG] Table header clicked: column=2, x=450, y=10
[DEBUG] Header rect: x=150, width=400, iconX=515, clickX=520
[INFO] Filter icon clicked for column 2
[INFO] === User Action: Image Name Filter Icon Clicked ===
```

## 构建信息

```bash
# 编译
mvn clean compile
# ✅ BUILD SUCCESS

# 打包
mvn package -DskipTests
# ✅ BUILD SUCCESS

# 输出
target/git-info-viewer-1.0.0-jar-with-dependencies.jar
```

## 修复总结

1. **禁用自动排序** - 解决点击 [F] 时触发排序的问题
2. **改进点击检测** - 确保过滤图标点击能被正确识别
3. **添加调试日志** - 方便排查问题
4. **扩大点击区域** - 提高用户体验

## 注意事项

### 关于排序功能

禁用自动排序后，用户无法通过点击列头来排序表格。如果需要排序功能，可以考虑：

1. **手动实现排序**
   - 只在点击列头文本区域时触发排序
   - 点击 [F] 图标区域时不触发排序

2. **添加排序按钮**
   - 在工具栏添加独立的排序按钮
   - 不与过滤功能冲突

3. **使用右键菜单**
   - 右键点击列头显示排序选项
   - 左键点击 [F] 图标进行过滤

### 当前实现

当前实现选择了**完全禁用排序**，因为：
- 过滤功能更重要
- 避免用户混淆
- 简化实现逻辑

如果用户需要排序功能，可以在后续版本中添加独立的排序控件。

## 下一步

建议用户：
1. 运行应用程序测试过滤功能
2. 验证所有三列的过滤图标都能正常工作
3. 确认不会触发排序
4. 如有需要，反馈是否需要恢复排序功能
