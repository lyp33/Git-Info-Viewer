# 列过滤功能修复 V2

## 问题描述

在之前的实现中，Image Name和Version列的过滤图标[F]点击后没有反应，无法弹出过滤对话框。

## 根本原因

问题出在鼠标事件监听器的实现上：
1. 使用了`mousePressed`事件，但在某些情况下这个事件可能被表头的其他组件拦截
2. 尝试使用`e.consume()`来阻止排序，但这在`mousePressed`中不够可靠
3. 自定义的表头渲染器（JPanel）可能干扰了鼠标事件的传递

## 解决方案

### 1. 改用`mouseClicked`事件
- 从`mousePressed`改为`mouseClicked`，这样可以更可靠地捕获完整的点击事件
- `mouseClicked`在鼠标按下并释放后触发，更适合处理用户交互

### 2. 支持右键点击
- 添加了右键点击支持，作为触发过滤的备选方式
- 用户可以右键点击任何有过滤功能的列标题来打开过滤对话框
- 这样即使左键点击图标区域有问题，右键点击也能工作

### 3. 增强调试输出
- 添加了更详细的调试信息，包括：
  - 鼠标按钮类型
  - 是否为右键点击
  - 点击位置的详细坐标

## 代码变更

### TenantCICDDialog.java

```java
// 修改前：使用 mousePressed
resultsTable.getTableHeader().addMouseListener(new java.awt.event.MouseAdapter() {
    @Override
    public void mousePressed(java.awt.event.MouseEvent e) {
        handleHeaderClick(e);
    }
});

// 修改后：使用 mouseClicked，支持右键点击
resultsTable.getTableHeader().addMouseListener(new java.awt.event.MouseAdapter() {
    @Override
    public void mouseClicked(java.awt.event.MouseEvent e) {
        handleHeaderClick(e);
    }
});
```

```java
// handleHeaderClick方法中的变更
// 添加右键点击检测
boolean isRightClick = SwingUtilities.isRightMouseButton(e);
boolean isInIconArea = e.getX() >= iconStartX;

// 右键点击或点击图标区域时触发过滤
if (isRightClick || isInIconArea) {
    System.out.println("Filter triggered for column " + column);
    
    // 处理过滤（移除了e.consume()调用）
    SwingUtilities.invokeLater(() -> {
        if (column == 2) {  // Image Name列
            handleImageNameFilter();
        } else if (column == 5) {  // Version列
            handleVersionFilter();
        } else if (column == 6) {  // Git Branch列
            handleBranchFilter();
        }
    });
}
```

## 测试方法

### 方法1：左键点击[F]图标
1. 运行应用程序
2. 打开Tenant CI/CD对话框
3. 连接到租户并执行搜索
4. 在结果表格中，点击以下列标题右侧的[F]图标：
   - Image Name列
   - Version列
   - Git Branch列
5. 应该弹出相应的过滤对话框

### 方法2：右键点击列标题（推荐）
1. 运行应用程序
2. 打开Tenant CI/CD对话框
3. 连接到租户并执行搜索
4. 在结果表格中，**右键点击**以下列的标题：
   - Image Name列
   - Version列
   - Git Branch列
5. 应该弹出相应的过滤对话框

### 查看调试输出
在控制台中查看以下调试信息：
```
=== Header Click Debug ===
Column: 2
Click X: 150
Click Y: 10
Button: 3
Is Right Click: true
Header Rect X: 100
Header Rect Width: 200
Header Rect Right: 300
Icon Start X: 260
Is in icon area: false
Filter triggered for column 2
```

## 功能说明

### Image Name列过滤
- 支持模糊匹配（不区分大小写）
- 输入关键词后，只显示包含该关键词的镜像名称

### Version列过滤
- 支持模糊匹配（不区分大小写）
- 输入关键词后，只显示包含该关键词的版本号

### Git Branch列过滤
- 从下拉列表中选择分支
- 支持输入过滤（实时过滤分支列表）
- 只显示选中分支的构建结果

### 组合过滤
- 三个过滤器可以同时使用
- 使用AND逻辑：结果必须同时满足所有已设置的过滤条件
- 过滤器图标颜色：
  - 灰色[F]：未设置过滤
  - 蓝色[F]：已设置过滤

### 清除过滤
- 在过滤对话框中点击"Clear"按钮
- 或者删除输入内容后点击"OK"

## 排序功能

- 点击列标题的文本部分（不是[F]图标）仍然可以触发排序
- 排序功能保持正常工作
- 右键点击会触发过滤，不会触发排序

## 编译和运行

```bash
# 编译
mvn clean compile

# 打包
mvn package -DskipTests

# 运行
java -jar target/git-info-viewer-1.0.0-jar-with-dependencies.jar
```

## 注意事项

1. **右键点击是最可靠的触发方式**：如果左键点击[F]图标没有反应，请使用右键点击列标题
2. **调试输出**：运行时会在控制台输出详细的点击事件信息，帮助诊断问题
3. **过滤前需要先搜索**：必须先执行搜索获取数据后，才能使用过滤功能

## 下一步

如果右键点击和左键点击都能正常工作，可以考虑：
1. 移除调试输出（System.out.println语句）
2. 优化图标点击区域的检测逻辑
3. 添加更明显的视觉反馈（如鼠标悬停时高亮[F]图标）
