# Git Info Viewer - 用户指南

## 目录

1. [简介](#简介)
2. [安装](#安装)
3. [快速开始](#快速开始)
4. [核心功能](#核心功能)
   - [仓库浏览器](#仓库浏览器)
   - [Git 操作](#git-操作)
   - [批量操作](#批量操作)
   - [GitLab 集成](#gitlab-集成)
   - [Jenkins 集成](#jenkins-集成)
   - [Portal CI/CD](#portal-cicd)
   - [部署管理](#部署管理)
5. [设置与配置](#设置与配置)
6. [使用技巧](#使用技巧)
7. [故障排除](#故障排除)

---

## 简介

**Git Info Viewer** 是一个为管理多个 Git 仓库的开发者设计的综合性 Java 桌面应用程序。它提供了统一的界面来查看仓库信息、执行批量操作，并与 GitLab、Jenkins 和 Portal CI/CD 系统集成。

### 主要特性

- 📁 **仓库浏览器** - 浏览和查看多个 Git 仓库
- 🔄 **批量操作** - 跨多个仓库切换分支、拉取更新
- 🍒 **Cherry-Pick 支持** - 在仓库之间传输提交
- 🦊 **GitLab 集成** - 从 GitLab 组克隆项目
- 🔨 **Jenkins 集成** - 监控和管理 Jenkins 构建
- 🚀 **Portal CI/CD** - 构建和部署应用程序
- 📊 **部署管理** - 监控 Kubernetes 部署
- 🎯 **版本模式自定义** - 灵活配置版本代码生成规则

---

## 安装

### 系统要求

- **Java 17 或更高版本**
- 本地可访问的 Git 仓库
- （可选）GitLab 账户用于 GitLab 集成
- （可选）Jenkins 访问权限用于 Jenkins 集成
- （可选）Portal API 访问权限用于 CI/CD 功能

### 安装步骤

1. 下载 `git-info-viewer-1.0.0-jar-with-dependencies.jar`
2. 将 JAR 文件放在方便的位置
3. 运行应用程序：
   ```bash
   java -jar git-info-viewer-1.0.0-jar-with-dependencies.jar
   ```

### 创建桌面快捷方式（Windows）

1. 右键点击桌面 → 新建 → 快捷方式
2. 输入位置：`javaw -jar "C:\path\to\git-info-viewer-1.0.0-jar-with-dependencies.jar"`
3. 命名为 "Git Info Viewer"

---

## 快速开始

### 首次启动

首次启动 Git Info Viewer 时，您会看到一个分割面板界面：

- **左侧面板**：目录树导航
- **右侧面板**：仓库信息和操作

```
┌─────────────────────────────────────────────────────┐
│  Git Info Viewer                            [_][□][X]│
├──────────────┬──────────────────────────────────────┤
│              │                                       │
│  目录树      │     仓库信息                          │
│              │                                       │
│  📁 项目     │     • 远程 URL                        │
│   📁 repo1   │     • 分支                            │
│   📁 repo2   │     • 提交历史                        │
│   📁 repo3   │     • 文件变更                        │
│              │                                       │
└──────────────┴──────────────────────────────────────┘
```

### 打开目录

1. 点击 **文件 → 打开目录**（或按 `Ctrl+O`）
2. 选择包含 Git 仓库的文件夹
3. 目录树将填充所有子目录
4. Git 仓库标记为 **[Git Repo]**

---

## 核心功能

### 仓库浏览器

#### 查看仓库信息

1. 在左侧面板中导航到目录
2. 点击 Git 仓库
3. 右侧面板显示：
   - **远程 URL** - Origin 和其他远程仓库
   - **当前分支** - 活动分支名称
   - **本地分支** - 所有本地分支
   - **远程分支** - 所有远程分支
   - **最近提交** - 最近 10 次提交及详情
   - **未提交的更改** - 修改、添加、删除的文件

#### 提交历史

每个提交显示：
- 提交哈希（短格式）
- 作者名称
- 提交日期和时间
- 提交消息
- 变更文件数量

**双击**提交可查看详细的文件变更。

---

### Git 操作

#### 切换分支

**单个仓库：**
1. 在树中选择仓库
2. 在右侧面板找到"当前分支"部分
3. 从下拉列表中选择分支
4. 点击 **Switch** 按钮
5. 应用程序将切换并拉取最新更改

**批量切换所有：**
1. 选择包含多个仓库的父目录
2. 勾选要切换的仓库
3. 在"批量切换所有"部分输入目标分支名称
4. 点击 **Switch All** 按钮
5. 在日志区域监控进度

```
┌─────────────────────────────────────────────┐
│  批量切换所有 Git 仓库                      │
├─────────────────────────────────────────────┤
│  目标分支: [dev          ] [Switch All]     │
└─────────────────────────────────────────────┘
```

#### 拉取更新

- **单个仓库**：在当前分支部分点击 **Pull** 按钮
- **批量拉取**：使用当前分支名称执行 Switch All

#### Cherry-Pick 操作

**单个提交 Cherry-Pick：**
1. 右键点击提交历史中的提交
2. 选择 **Cherry-Pick 到另一个仓库**
3. 选择目标仓库
4. 选择目标分支
5. 点击 **Cherry-Pick**

**批量 Cherry-Pick：**
1. 点击 **批量 Cherry-Pick** 按钮
2. 选择源仓库和分支
3. 选择要 cherry-pick 的提交
4. 选择目标仓库
5. 点击 **开始 Cherry-Pick**
6. 在摘要对话框中查看结果

---

### 批量操作

#### 选择多个仓库

使用复选框选择仓库进行批量操作：

```
☑ repo1      [Git Repo]  master  origin  2025-01-20  author1  Switch
☑ repo2      [Git Repo]  dev     origin  2025-01-19  author2  Switch
☐ repo3      [Git Repo]  master  origin  2025-01-18  author3  Switch
```

#### 批量切换所有

1. 勾选要包含的仓库
2. 输入目标分支名称
3. 点击 **Switch All**
4. 在日志中查看进度：
   ```
   开始批量切换到分支: dev
   ========================================
   [1] 处理中: repo1
     ✓ 分支已切换到 dev
     正在拉取最新更改...
     ✓ 拉取成功完成！
   [2] 处理中: repo2
     ✓ 分支已切换到 dev
     ✓ 拉取成功完成！
   ========================================
   批量操作完成！
   已处理: 2, 成功: 2, 失败: 0
   ```

---

### GitLab 集成

#### 配置 GitLab 设置

1. 转到 **设置 → GitLab 设置**
2. 输入您的凭据：
   - **私有令牌**（推荐）- GitLab 个人访问令牌
   - 或 **用户名** 和 **密码**
3. 点击 **保存**

**创建 GitLab 个人访问令牌：**
1. 转到 GitLab → 设置 → 访问令牌
2. 创建具有 `api` 和 `read_repository` 范围的令牌
3. 复制令牌并粘贴到 Git Info Viewer

#### 检出 GitLab 项目

**单个仓库：**
1. 点击 **文件 → 检出新 Git 项目**
2. 输入 Git URL：`https://gitlab.example.com/user/repo.git`
3. 点击 **检查** 获取分支
4. 选择分支
5. 点击 **下载**

**GitLab 组（批量克隆）：**
1. 点击 **文件 → 检出新 Git 项目**
2. 输入组 URL：`https://gitlab.example.com/group-name`
3. 点击 **检查**
4. 查看组中的项目列表
5. 勾选要下载的项目
6. 选择分支（应用于所有项目）
7. 点击 **下载**

```
┌─────────────────────────────────────────────────────┐
│  检出新 Git 项目                                    │
├─────────────────────────────────────────────────────┤
│  Git URL: [https://gitlab.example.com/group  ][检查]│
│  分支:    [master ▼]                        [下载]  │
│                                                      │
│  组项目                              找到 13 个项目  │
│  ┌──────────────────────────────────────────────┐  │
│  │☑ project1        project1                    │  │
│  │☑ project2        project2                    │  │
│  │☐ project3        project3                    │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**提示：** 您可以调整对话框大小以查看列表中的更多项目。

---

### Jenkins 集成

#### 配置 Jenkins

1. 转到 **设置 → Jenkins 设置**
2. 输入 Jenkins URL：`http://jenkins.example.com`
3. 输入用户名和 API 令牌
4. 点击 **保存**

**获取 Jenkins API 令牌：**
1. 转到 Jenkins → 用户 → 配置
2. 点击"添加新令牌"
3. 复制令牌

#### 浏览 Jenkins 任务

1. 点击 **工具 → Jenkins 浏览器**
2. 导航任务树
3. 查看任务详情：
   - 构建历史
   - 构建参数
   - 控制台输出
   - 阶段视图（Pipeline 任务）

#### 查看构建详情

**构建历史：**
- 双击构建查看控制台输出
- 右键点击 **重新构建** 使用相同参数

**阶段视图（Pipeline 任务）：**
- 查看所有 pipeline 阶段
- 点击阶段查看日志
- 查看阶段持续时间和状态
- 查看并行阶段

**控制台输出：**
- 带语法高亮的完整构建日志
- 搜索功能（Ctrl+F）
- 自动滚动到底部
- 复制选定文本

---

### Portal CI/CD

#### 配置 Portal 设置

1. 转到 **设置 → Portal 设置**
2. 输入 Portal URL：`https://portal.example.com`
3. 输入客户端 ID 和密钥
4. 点击 **保存**

#### 租户 CI/CD 管理

1. 点击 **工具 → 租户 CI/CD**
2. 从下拉列表中选择租户
3. 应用程序自动连接

**查询构建结果：**
- **计划名称**：按计划名称过滤
- **应用名称**：按应用程序过滤（支持自动完成）
- **创建者**：按创建者邮箱过滤
- **页面大小**：结果数量（默认：10）
- 点击 **搜索**

**构建结果表：**
```
┌────────────────────────────────────────────────────────────────┐
│ 应用名称    │ 镜像名称        │ 构建状态     │ 创建时间       │
├────────────────────────────────────────────────────────────────┤
│ app-bff     │ docker.../app:v1│ 构建成功     │ 2025-01-20 10:30│
│ app-service │ docker.../app:v2│ 构建失败     │ 2025-01-20 09:15│
└────────────────────────────────────────────────────────────────┘
```

**操作：**
- **双击**行查看构建输出
- **右键点击** **重新构建**（成功和失败都可以）
- **悬停**在行上 1 秒查看详细提示
- **Ctrl+C** 复制选定的单元格
- 使用过滤图标按 Git 分支**过滤**

#### 构建应用程序

**单个应用程序构建：**
1. 在租户 CI/CD 对话框中，点击 **构建镜像**
2. 选择应用程序
3. 选择分支
4. 输入版本代码
5. 点击 **构建**

**批量构建（Build Package）：**
1. 点击 **构建镜像** 按钮
2. 选择分支（支持自动完成）
3. 版本代码自动生成
4. 从两个列表中选择应用程序：
   - **未收藏的应用程序**（左侧）
   - **已收藏的应用程序**（右侧）
5. 点击 **Build Package**

**管理收藏：**
- 点击 **→** 将选定的应用添加到收藏
- 点击 **←** 从收藏中移除
- 在列表之间拖动应用
- 创建组来组织收藏
- 将应用拖入组

**收藏分组：**
```
┌─────────────────────────────────────────┐
│ 已收藏的应用程序                        │
├─────────────────────────────────────────┤
│ [-] 后端服务 (3)                        │
│   ☐ app-bff                             │
│   ☐ app-service                         │
│   ☐ app-api                             │
│ [+] 前端应用 (2)                        │
│ [-] 未分组 (1)                          │
│   ☐ app-admin                           │
└─────────────────────────────────────────┘
```

**分组操作：**
- 右键点击组标题以重命名或删除
- 右键点击收藏区域以添加新组
- 拖动应用在组内重新排序
- 在组之间拖动应用
- 从左侧列表拖动到特定组

#### 版本代码模式自定义 🆕

**配置版本模式：**
1. 在 Build Package 对话框中
2. 在"Version Code/Plan Code"标签旁边，点击蓝色的模式链接
3. 在弹出的对话框中：
   - 输入自定义模式（使用占位符）
   - 查看实时预览
   - 参考帮助文本了解可用占位符
4. 点击 **Save** 保存配置
5. 版本代码将自动使用新模式生成

**可用占位符：**

| 占位符 | 说明 | 示例输出 |
|--------|------|----------|
| `{branch}` | 分支名称 | `master`, `develop` |
| `{YYYY}` | 4位年份 | `2026` |
| `{MM}` | 2位月份 | `02` |
| `{DD}` | 2位日期 | `06` |
| `{HH}` | 2位小时 | `18` |
| `{MI}` | 2位分钟 | `42` |
| `{SS}` | 2位秒数 | `39` |
| `{YYYYMMDD}` | 完整日期 | `20260206` |
| `{HHMMSS}` | 完整时间 | `184239` |
| `{YYYYMMDDHHMMSS}` | 完整日期时间 | `20260206184239` |

**模式示例：**

```
# 简单格式
{branch}_{YYYYMMDD}
输出: master_20260206

# 详细格式
v{YYYY}.{MM}.{DD}-{branch}-{HHMMSS}
输出: v2026.02.06-master-184239

# 紧凑格式
{branch}{YYYYMMDDHHMMSS}
输出: master20260206184239

# 自定义分隔符
{branch}_build_{YYYYMMDD}_{HHMMSS}
输出: master_build_20260206_184239
```

**默认模式：**
```
{branch}_{YYYYMMDDHHMMSS}
```

**特性：**
- ✅ 每个租户独立配置
- ✅ 实时预览效果
- ✅ 自动验证模式
- ✅ 无效模式自动回退到默认格式
- ✅ 配置持久化保存
- ✅ 分支变更自动重新生成版本代码

#### 自动刷新

启用自动刷新以监控构建进度：
1. 勾选 **自动刷新** 复选框
2. 设置间隔秒数（默认：10）
3. 搜索结果自动刷新

---

### 部署管理

#### 查看部署

1. 点击 **工具 → 租户 CI/CD**
2. 点击 **部署** 按钮
3. 选择环境
4. 查看部署列表

**部署信息：**
- 应用程序名称
- 镜像版本
- 部署状态
- Pod 数量
- 最后更新时间

#### 查看 Pod 日志

1. 选择部署
2. 点击 **查看 Pods**
3. 选择 Pod
4. 点击 **查看日志**
5. 日志实时显示

**日志功能：**
- 在日志中搜索（Ctrl+F）
- 自动滚动到底部
- 复制选定文本
- 刷新日志

---

## 设置与配置

### 常规设置

**文件 → 设置**

- **左侧面板字体**：目录树的字体
- **右侧面板字体**：仓库信息的字体
- **重置为默认**：恢复默认字体

### GitLab 设置

**设置 → GitLab 设置**

配置 GitLab 集成：
- **私有令牌**：个人访问令牌（推荐）
- **用户名**：GitLab 用户名
- **密码**：GitLab 密码

**优先级：** 私有令牌 > 用户名/密码

### Jenkins 设置

**设置 → Jenkins 设置**

配置 Jenkins 集成：
- **Jenkins URL**：基础 URL（例如：`http://jenkins.example.com`）
- **用户名**：Jenkins 用户名
- **API 令牌**：Jenkins API 令牌

### Portal 设置

**设置 → Portal 设置**

配置 Portal CI/CD：
- **Portal URL**：基础 URL（例如：`https://portal.example.com`）
- **客户端 ID**：API 客户端 ID
- **客户端密钥**：API 客户端密钥

---

## 使用技巧

### 键盘快捷键

- `Ctrl+O` - 打开目录
- `Ctrl+F` - 在当前视图中搜索
- `Ctrl+C` - 复制选定的文本/单元格
- `F5` - 刷新当前视图

### 高效工作流程

**日常分支切换：**
1. 在设置中配置一次 GitLab 凭据
2. 使用"Switch All"进行批量操作
3. 凭据在会话期间被记住

**监控构建：**
1. 在租户 CI/CD 中启用自动刷新
2. 将间隔设置为 10-30 秒
3. 保持窗口打开以监控进度

**组织收藏：**
1. 为不同的服务创建组（后端、前端等）
2. 将应用拖入适当的组
3. 组按租户保存

**版本代码管理：**
1. 为每个租户配置合适的版本模式
2. 使用分支名称占位符区分不同分支的构建
3. 使用日期时间占位符确保版本唯一性
4. 模式配置后自动应用，无需手动输入

### 性能提示

- 使用过滤器减少结果集
- 关闭未使用的对话框
- 限制大数据集的页面大小

---

## 故障排除

### 常见问题

**认证错误**

**问题：** 使用 Switch All 时"认证失败"

**解决方案：**
1. 转到设置 → GitLab 设置
2. 输入您的 GitLab 用户名和密码
3. 点击保存
4. 再次尝试 Switch All

**问题：** Jenkins 认证失败

**解决方案：**
1. 验证 Jenkins URL 是否正确
2. 检查 API 令牌是否有效
3. 确保用户具有必要的权限

**构建失败**

**问题：** 构建失败，错误不清楚

**解决方案：**
1. 双击构建行查看输出
2. 检查构建日志中的错误消息
3. 验证分支存在且可访问
4. 检查应用程序配置

**版本模式问题**

**问题：** 版本代码生成不符合预期

**解决方案：**
1. 点击版本代码旁边的蓝色链接检查当前模式
2. 在配置对话框中查看实时预览
3. 确认占位符拼写正确（区分大小写）
4. 检查是否使用了无效的占位符
5. 如果模式无效，系统会自动使用默认格式

**连接问题**

**问题：** 无法连接到 Portal/Jenkins

**解决方案：**
1. 验证 URL 是否正确（包括 http:// 或 https://）
2. 检查网络连接
3. 验证防火墙设置
4. 检查凭据是否正确

### 获取帮助

**日志文件：**
- 应用程序日志打印到控制台
- 使用控制台运行以查看详细日志：
  ```bash
  java -jar git-info-viewer-1.0.0-jar-with-dependencies.jar
  ```

**调试模式：**
- 检查控制台输出以获取详细的错误消息
- 查找堆栈跟踪
- 记录失败的操作

---

## 附录

### 支持的 Git 操作

- ✅ 克隆仓库
- ✅ 切换分支
- ✅ 拉取更新
- ✅ Cherry-pick 提交
- ✅ 查看提交历史
- ✅ 查看文件差异
- ✅ 批量操作

### 支持的 CI/CD 操作

- ✅ 构建单个应用程序
- ✅ 构建多个应用程序（打包）
- ✅ 重新构建失败/成功的构建
- ✅ 查看构建输出
- ✅ 监控构建状态
- ✅ 部署应用程序
- ✅ 查看部署日志
- ✅ 自定义版本代码模式

### 系统要求

- **操作系统**：Windows、macOS、Linux
- **Java**：17 或更高版本
- **内存**：最低 512 MB，推荐 1 GB
- **磁盘**：应用程序 100 MB
- **网络**：GitLab、Jenkins、Portal 集成需要

---

## 版本历史

### 版本 1.0.0（当前）

**功能：**
- 仓库浏览器和 Git 操作
- 批量分支切换
- Cherry-pick 支持
- GitLab 集成
- Jenkins 集成
- Portal CI/CD 集成
- 部署管理
- 带拖放的收藏分组
- 构建自动刷新
- 成功构建的重新构建
- 可调整大小的对话框
- **版本代码模式自定义** 🆕
  - 10 种占位符支持
  - 实时预览
  - 租户隔离配置
  - 自动验证和回退

**错误修复：**
- 修复了认证未使用配置的凭据
- 修复了组项目列表大小
- 修复了重新构建仅适用于失败构建
- 修复了版本模式持久化问题

---

## 联系与支持

如有问题、疑问或功能请求，请联系您的开发团队。

**祝您 Git 管理愉快！ 🚀**
